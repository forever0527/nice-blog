---
title: CSS 动画艺术：从过渡到 3D
desc: 深入探索现代 CSS 动画的各种技法，从简单的 transition 到令人惊叹的 3D 变换。
date: 2026-05-12
tag: UI设计
color: "#00d2ff"
img: /img/bg/2.png
---
CSS 动画不再只是简单的 hover 效果。现代浏览器支持 3D 变换、自定义缓动函数、滚动驱动动画等高级特性。

## 基础过渡

`transition` 是最简单的动画方式，但配合 cubic-bezier 缓动函数能产生非常自然的交互效果。

## 关键帧动画

`@keyframes` 提供了对动画每一帧的精确控制，配合 `animation-timeline` 还能实现滚动驱动。

## 3D 世界

`perspective` 和 `transform-style: preserve-3d` 开启了 CSS 的第三维度，可以创造出令人惊叹的视觉效果。
